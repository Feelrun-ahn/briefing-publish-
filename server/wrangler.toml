name = "briefing-api"
main = "worker.js"
compatibility_date = "2026-08-01"

# 인증키는 파일에 적지 않는다. 아래 명령으로 넣는다.
#   wrangler secret put NEIS_KEY

# 사용량을 계속 남기려면 (선택)
#   wrangler kv namespace create USAGE
# 위 명령이 알려 주는 id 를 아래에 넣고 주석을 푸세요.
#
# [[kv_namespaces]]
# binding = "USAGE"
# id = "여기에-id"
